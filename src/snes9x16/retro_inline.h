/* Device port: libretro-common's retro_inline.h is not vendored here. The
   core only ever needs the INLINE spelling. */
#ifndef __RETRO_INLINE_H
#define __RETRO_INLINE_H
#ifndef INLINE
#define INLINE inline
#endif
#endif
