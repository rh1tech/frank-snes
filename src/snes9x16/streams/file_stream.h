/* Stub: see file_stream_transforms.h. zipfile.h wants this; ZIP loading is
 * not built on the device, the ROM comes off SD through the port's own
 * loader. */
#ifndef PORT_FILE_STREAM_H
#define PORT_FILE_STREAM_H
#include "file_stream_transforms.h"
#endif
